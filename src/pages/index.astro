---
const title = "PEAAII 2026 AI Summer Camp";

/**
 * Edit these placeholders and you’re done.
 * You can also move these into a separate file later, but keeping it simple is totally fine.
 */
const camp = {
  org: "Paul English Applied Artificial Intelligence Institute (PEAAII)",
  name: "AI Summer Camp",
  year: "2026",

  // Summer Camp Info
  tagline: "A hands-on introduction to AI through guided labs and team projects.",
  description:
    "The Paul English Applied AI Institute's summer camp is designed to be a modular, flexible AI camp that allows for both short-term participation and a continuous multi-week learning experience.  Each week will cover a different topic - Week 1 acts as an introduction to AI and Machine Learning, Week 2 covers Neural Networks and Computer Vision, Week 3 details Natural Language Processing, Week 4 teaches about Generative AI and Creativity, and the final topic for Week 5 looks inside intelligence - a journey into minds and machines.",
  bullets: [
    "Week 1 - Train a simple image classifier or chatbot",
    "Week 2 - Real-time object detection on Raspberry PI Vision-guided robotic arm for object sorting",
    "Week 3 - Text sentiment analyzer or story generator",
    "Week 4 - Generate art, music, or memes",
    "Week 5 - Group project tackling a real-world problem",
  ],

  // General info (placeholders)
  quickFacts: [
    { label: "Dates", value: "July 27th - August 28th, Monday - Friday" },
    { label: "Time", value: "9:00 AM - 4:00 PM" },
    { label: "Location", value: "UMass Boston, Room: TBD" },
    { label: "Audience", value: "Grades 8–12" },
  ],

  // Registration Info
  registration: {
    price: "TBD",
    deadline: "TBD",
    capacity: "TBD",
    formUrl: "https://forms.gle/xtcpMArT4BBZ6CRK7", // paste the Microsoft Form link here
    note:
      "Please apply via our Microsoft Application form.",
  },

  // Contact
  contactEmail: "ai@umb.edu", // when they click "contact" this will be the default email that the email platform primes to send to
};

const team = [
  {
    name: "Professor Wei Ding",
    role: "Executive Director, PEAAII",
    photoUrl: "", // optional: "/images/prof1.jpg"
    bio:
      "Dr. Ding combines expertise in AI, data mining, machine learning, and ​computational semantics—applied in healthcare and environmental, astronomical, and geological sciences—with a deep ​knowledge of UMass Boston and a strong connection to students, gained over 14 years on the faculty.  As the executive director, she facilitates the PEAAII Lead Planning Team, coordinates the curricular working group, and supports and ​consults with the Student Advisory Council. Wei balances leading the institute with teaching ​while also fulfilling significant responsibilities as PI on significant externally sponsored research projects.",
  },
  {
    name: "Professor Ping Chen",
    role: "Professor",
    photoUrl: "",
    bio:
      "Dr. Ping Chen is a Full Professor of Computer Engineering and the Director of Artificial Intelligence Lab at the University of Massachusetts Boston. His research interests include Artificial Intelligence, Natural Language Processing, and Machine Learning. Dr. Chen has published over 100 papers in major Data Mining, Artificial Intelligence, and Machine Learning conferences and journals.",
  },
  {
    name: "Professor Xiaohui Liang",
    role: "Professor",
    photoUrl: "",
    bio:
      "",
  },
  {
    name: "Professor Shichao Pei",
    role: "Professor",
    photoUrl: "",
    bio:
      "Dr. Pei is an Assistant Professor in the Department of Computer Science at the University of Massachusetts Boston. His research interests broadly lie in machine learning and AI, in particular foundation models, AI safety, AI agent.",
  },
  {
    name: "Professor Bo Sheng",
    role: "Professor",
    photoUrl: "",
    bio:
      "Dr. Bo Sheng is a professor of computer science at UMass Boston. His research interests include edge computing, mobile computing, cloud computing, and cybersecurity. He directs the newly funded Networked AI Lab, focusing on robotics and collaborative AI.",
  },
  {
    name: "Professor Yinxin Wan",
    role: "Professor",
    photoUrl: "",
    bio:
      "Dr. Wan is an Assistant Professor in the Department of Computer Science at UMass Boston. His research interests include secure and trustworthy AI, IoT, and data-driven networked systems.",
  },
  {
    name: "Chengjie Zheng",
    role: "Graduate Student Assistant",
    photoUrl: "",
    bio:
      "",
  },
  {
    name: "Ajanee Igharo",
    role: "Student Assistant",
    photoUrl: "",
    bio:
      "",
  },
  {
    name: "Dora Nguyen",
    role: "Student Assistant",
    photoUrl: "",
    bio:
      "",
  },
  {
    name: "Rebaz Kamal",
    role: "Student Assistant",
    photoUrl: "",
    bio:
      "",
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta
      name="description"
      content="Official website of the Paul English Applied AI Institute (PEAAII) 2026 AI Summer Camp."
    />

    <style>
      :root {
        --bg: #0b1020;
        --surface: rgba(255, 255, 255, 0.06);
        --surface-2: rgba(255, 255, 255, 0.1);
        --text: #e5e7eb;
        --muted: rgba(229, 231, 235, 0.75);
        --border: rgba(255, 255, 255, 0.12);
        --shadow: 0 18px 60px rgba(0, 0, 0, 0.45);

        --brand-1: #60a5fa; /* blue */
        --brand-2: #a78bfa; /* violet */
        --accent: #34d399; /* green */
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        color: var(--text);
        background: radial-gradient(
            900px 500px at 15% 10%,
            rgba(96, 165, 250, 0.26),
            transparent 60%
          ),
          radial-gradient(
            800px 450px at 90% 15%,
            rgba(167, 139, 250, 0.22),
            transparent 55%
          ),
          radial-gradient(
            900px 600px at 35% 100%,
            rgba(52, 211, 153, 0.18),
            transparent 60%
          ),
          linear-gradient(180deg, #070a14, #0b1020);
        min-height: 100vh;
      }

      a { color: inherit; }

      .container {
        max-width: 1050px;
        margin: 0 auto;
        padding: 0 1.25rem;
      }

      /* Top nav */
      .topbar { padding: 1rem 0; }
      .topbar-inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }
      .brand {
        display: flex;
        flex-direction: column;
        line-height: 1.15;
      }
      .brand strong { font-size: 0.95rem; letter-spacing: 0.01em; }
      .brand span { font-size: 0.85rem; color: var(--muted); }

      .navlinks {
        display: flex;
        gap: 0.75rem;
        flex-wrap: wrap;
        align-items: center;
      }

      .navlinks a {
        text-decoration: none;
        padding: 0.45rem 0.75rem;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.06);
        color: var(--muted);
        font-size: 0.9rem;
      }
      .navlinks a:hover { background: rgba(255, 255, 255, 0.1); color: var(--text); }

      /* Hero */
      header.hero { padding: 1.5rem 0 1.25rem; }

      .hero-card {
        border: 1px solid var(--border);
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.08),
          rgba(255, 255, 255, 0.04)
        );
        border-radius: 20px;
        padding: 2rem;
        box-shadow: var(--shadow);
        position: relative;
        overflow: hidden;
      }

      .hero-card:before {
        content: "";
        position: absolute;
        inset: -2px;
        background: radial-gradient(
            550px 220px at 20% 0%,
            rgba(96, 165, 250, 0.35),
            transparent 60%
          ),
          radial-gradient(
            450px 220px at 90% 10%,
            rgba(167, 139, 250, 0.28),
            transparent 55%
          );
        pointer-events: none;
        opacity: 0.9;
      }

      .hero-grid {
        position: relative;
        display: grid;
        grid-template-columns: 1.3fr 0.7fr;
        gap: 1.5rem;
        align-items: start;
      }

      @media (max-width: 900px) {
        .hero-grid { grid-template-columns: 1fr; }
      }

      h1 {
        margin: 0.4rem 0 0.75rem;
        font-size: clamp(2rem, 4vw, 3rem);
        letter-spacing: -0.02em;
      }

      .subhead {
        margin: 0 0 1.25rem;
        color: var(--muted);
        font-size: 1.05rem;
        line-height: 1.6;
        max-width: 65ch;
      }

      .cta-row {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
        margin-top: 1rem;
      }

      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        border-radius: 12px;
        padding: 0.75rem 1rem;
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.08);
        text-decoration: none;
        font-weight: 650;
        transition: transform 120ms ease, background 120ms ease;
      }

      .btn:hover {
        transform: translateY(-1px);
        background: rgba(255, 255, 255, 0.11);
      }

      .btn.primary {
        border-color: rgba(96, 165, 250, 0.35);
        background: linear-gradient(
          135deg,
          rgba(96, 165, 250, 0.28),
          rgba(167, 139, 250, 0.22)
        );
      }

      .btn.primary:hover {
        background: linear-gradient(
          135deg,
          rgba(96, 165, 250, 0.34),
          rgba(167, 139, 250, 0.28)
        );
      }

      .info-box {
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.06);
        border-radius: 16px;
        padding: 1rem;
      }

      .info-title {
        margin: 0 0 0.75rem;
        font-size: 0.95rem;
        color: rgba(229, 231, 235, 0.95);
      }

      .info-list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        gap: 0.65rem;
      }

      .info-item {
        display: grid;
        grid-template-columns: 90px 1fr;
        gap: 0.75rem;
        align-items: start;
        padding: 0.65rem 0.75rem;
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
      }

      .info-item strong {
        color: var(--muted);
        font-size: 0.85rem;
        font-weight: 650;
      }

      /* Sections */
      main { padding: 0.5rem 0 2.5rem; }

      section { margin-top: 1.75rem; }

      .section-head {
        display: flex;
        align-items: baseline;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
        margin-bottom: 0.75rem;
      }

      .section-title {
        margin: 0;
        font-size: 1.2rem;
        letter-spacing: -0.01em;
      }

      .section-note {
        margin: 0;
        color: var(--muted);
        font-size: 0.95rem;
      }

      .card {
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.06);
        border-radius: 16px;
        padding: 1rem;
      }

      .grid-3 {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
      }
      @media (max-width: 950px) {
        .grid-3 { grid-template-columns: 1fr; }
      }

      .stack {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
    
      .stack .card {
        max-width: 900px;
      }

      .bullets {
        margin: 0.5rem 0 0;
        padding-left: 1.15rem;
        color: var(--muted);
        line-height: 1.6;
      }

      /* Registration */
      .reg {
        border: 1px solid rgba(96, 165, 250, 0.35);
        background: linear-gradient(
          135deg,
          rgba(96, 165, 250, 0.18),
          rgba(167, 139, 250, 0.12)
        );
        border-radius: 18px;
        padding: 1.25rem;
        display: grid;
        grid-template-columns: 1fr auto;
        gap: 1rem;
        align-items: center;
      }

      @media (max-width: 700px) {
        .reg { grid-template-columns: 1fr; }
      }

      .reg h3 { margin: 0 0 0.25rem; font-size: 1.05rem; }
      .reg p { margin: 0.25rem 0 0; color: rgba(229,231,235,0.85); line-height: 1.55; }

      .reg-meta {
        display: grid;
        gap: 0.5rem;
        margin-top: 0.5rem;
        color: var(--muted);
        font-size: 0.95rem;
      }

      .reg-meta span strong { color: rgba(229,231,235,0.95); }

      /* Team cards */
      .team-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
      }
      @media (max-width: 1000px) {
        .team-grid { grid-template-columns: 1fr; }
      }

      .person {
        display: grid;
        grid-template-columns: 64px 1fr;
        gap: 0.9rem;
        align-items: start;
      }

      .avatar {
        width: 64px;
        height: 64px;
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.14);
        background: rgba(255, 255, 255, 0.08);
        display: grid;
        place-items: center;
        color: rgba(229,231,235,0.85);
        font-weight: 800;
        letter-spacing: -0.02em;
        overflow: hidden;
      }

      .avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .person h3 {
        margin: 0;
        font-size: 1.02rem;
      }

      .role {
        margin: 0.15rem 0 0.55rem;
        color: var(--muted);
        font-size: 0.92rem;
      }

      .bio {
        margin: 0;
        color: rgba(229,231,235,0.82);
        line-height: 1.6;
      }

      /* Footer */
      footer {
        padding: 2rem 0 2.5rem;
        color: rgba(229, 231, 235, 0.65);
        font-size: 0.9rem;
        text-align: center;
      }

      .fineprint {
        margin-top: 0.5rem;
        color: rgba(229, 231, 235, 0.55);
        font-size: 0.85rem;
      }
    </style>
  </head>

  <body>
    <!-- Top bar -->
    <div class="container topbar">
      <div class="topbar-inner">
        <div class="brand">
          <strong>{camp.org}</strong>
          <span>{camp.year} {camp.name}</span>
        </div>

        <nav class="navlinks" aria-label="Page sections">
          <a href="#camp-info">Summer Camp Info</a>
          <a href="#registration">Application</a>
          <a href="#team">Meet the Team</a>
        </nav>
      </div>
    </div>

    <!-- Hero -->
    <header class="hero">
      <div class="container">
        <div class="hero-card">
          <div class="hero-grid">
            <div>
              <h1>{camp.year} {camp.name}</h1>
              <p class="subhead">{camp.tagline}</p>

              <div class="cta-row">
                <a class="btn primary" href="#registration">Apply</a>
                <a class="btn" href="#camp-info">Learn more</a>
                <a class="btn" href={"mailto:" + camp.contactEmail}>Contact</a>
              </div>
            </div>

            <aside class="info-box" aria-label="Quick facts">
              <p class="info-title">Quick facts</p>
              <ul class="info-list">
                {camp.quickFacts.map((item) => (
                  <li class="info-item">
                    <strong>{item.label}</strong>
                    <span>{item.value}</span>
                  </li>
                ))}
              </ul>
            </aside>
          </div>
        </div>
      </div>
    </header>

    <main>
      <div class="container">
        <!-- 1) Summer Camp Info -->
        <section id="camp-info" aria-label="Summer Camp Info">
          <div class="section-head">
            <h2 class="section-title">Summer Camp Info</h2>
          </div>

          <div class="stack">
            <div class="card">
              <h3>About the camp:</h3>
              <p class="section-note">{camp.description}</p>
            </div>

            <div class="card">
              <h3>What Students will do (examples):</h3>
              <ul class="bullets">
                {camp.bullets.map((b) => (
                  <li>{b}</li>
                ))}
              </ul>
            </div>

            <div class="card">
              <h3>Good to know:</h3>
              <p class="section-note">
                Students will get the most out of attending all five weeks of our camp - however,
                parents should also feel comfortable knowing that the student can begin our summer camp
                with <em>any</em> of the listed weeks and will be able to continue along with the
                other students who may have already been attending the camp.  In other words, no student
                or parent should worry that the student would be "behind" in the camp by starting in a 
                week other than week one. <br><br>

                However, students who begin with the first week and continue through the entire camp may have
                exclusive opportunities, such as beginning a certain type of project in the first week and expanding
                on it each subsequent week, having completed a five-week project by the end of the camp.
              </p>
            </div>
          </div>
        </section>

        <!-- 2) Registration Info -->
        <section id="registration" aria-label="Registration Info">
          <div class="section-head">
            <h2 class="section-title">Application</h2>
            <p class="section-note">Pricing, deadlines, and the official application link</p>
          </div>

          <div class="reg">
            <div>
              <h3>Apply via Microsoft Application Form</h3>
              <p>{camp.registration.note}</p>

              <div class="reg-meta">
                <span><strong>Price:</strong> {camp.registration.price}</span>
                <span><strong>Deadline:</strong> {camp.registration.deadline}</span>
                <span><strong>Capacity:</strong> {camp.registration.capacity}</span>
              </div>
            </div>

            <a
              class="btn primary"
              href={camp.registration.formUrl}
              target="_blank"
              rel="noopener noreferrer"
            >
              Open Application Form
            </a>
          </div>
        </section>

        <!-- 3) Professor/Assistant bios -->
        <section id="team" aria-label="Professor and Assistant Bios">
          <div class="section-head">
            <h2 class="section-title">Meet the Team</h2>
            <p class="section-note">Professors and Student Assistants running the camp:</p>
          </div>

          <div class="team-grid">
            {team.map((p) => (
              <div class="card">
                <div class="person">
                  <div class="avatar" aria-hidden="true">
                    {p.photoUrl ? (
                      <img src={p.photoUrl} alt={`${p.name} photo`} />
                    ) : (
                      <span>{p.name.split(" ").map((x) => x[0]).join("").slice(0, 2).toUpperCase()}</span>
                    )}
                  </div>

                  <div>
                    <h3>{p.name}</h3>
                    <p class="role">{p.role}</p>
                    {p.bio ? <p class="bio">{p.bio}</p> : <p class="bio" style="color: var(--muted);">Bio coming soon.</p>}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </section>
      </div>
    </main>

    <footer>
      <div class="container">
        © {camp.year} {camp.org}
        <div class="fineprint">
          Questions? Email <a href={"mailto:" + camp.contactEmail}>{camp.contactEmail}</a>
        </div>
      </div>
    </footer>
  </body>
</html>

